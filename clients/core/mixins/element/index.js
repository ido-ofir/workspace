var React = require('react');
// var PropTypes = React.PropTypes;

// var core = require('core');
var styleMixin = require('../style');
var domMixin = require('../dom');

module.exports = {
  mixins: [styleMixin, domMixin]
};
