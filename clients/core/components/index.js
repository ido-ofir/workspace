var layout = require('./layout');
var forms = require('./forms');
var ui = require('./ui');
var widgets = require('./widgets');

module.exports = {
  layout: layout,
  forms: forms,
  ui: ui,
  widgets: widgets
};
